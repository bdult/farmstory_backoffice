<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="contentsQuery">


	<select id="list" resultType="hashmap">
		SELECT
			*
		FROM  BG_ITEM
	</select>
	
	<select id="listBySeriseId" resultType="hashmap">
		SELECT
			BI.*
		FROM BG_SERIES BS, BG_ITEM BI
		WHERE 1=1
		AND BS.IDX= #{serise_idx}
		AND BI.SERIES_ID = BS.SERIES_ID
	</select>
	
	
	<insert id="create" parameterType="map">
   		INSERT BG_ITEM(
			ITEM_ID,
			ITEM_NM,
			BRAND_ID,
			SRC_PATH   		
   		)
   		VALUES(
   			'${item_id}',
   			'${item_nm}',
   			'${brand_id}',
   			'${src_path}'
   		)
   </insert>
   
   <delete id="deleteByGroupId" >
   		DELETE FROM BG_ITEM
   		WHERE GROUP_ID = #{groupId}
   </delete>
	
</mapper>
