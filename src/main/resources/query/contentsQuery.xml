<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="contentsQuery">


	<select id="list" resultType="hashmap">
		SELECT
			*
		FROM  BG_CONTENTS
	</select>
	
	<select id="detail" parameterType="com.bgg.farmstoryback.dto.ContentsDTO" resultType="com.bgg.farmstoryback.dto.ContentsDTO">
		SELECT
			*
		FROM  BG_CONTENTS
		WHERE CONTENTS_ID = #{contents_id}
	</select>
	
	
	<insert id="create" parameterType="map"  keyProperty="contents_id">
   		INSERT BG_CONTENTS(
			CONTENTS_NM,
			CONTENTS_SERIES_ID,
			BRAND_ID,
			SRC_PATH,
			REG_DT   		
   		)
   		VALUES(
   			#{contents_nm},
   			#{contents_series_id},
   			#{brand_id},
   			#{src_path},
   			now()
   		)
   </insert>
   
   <update id="modify">
   		UPDATE BG_CONTENTS SET
   			ITEM_NM = #{item_nm}
   			, SRC_PATH = #{src_path}
   			, CONTENTS_SERIES_ID = #{contents_series_id}
   			, MOD_DT = now()
   		WHERE 1=1
   		AND CONTENTS_ID = #{contents_id}
   </update>
   
   <delete id="delete" >
   		DELETE FROM BG_CONTENTS
   		WHERE CONTENTS_ID = #{contentsID}
   </delete>
	
</mapper>
