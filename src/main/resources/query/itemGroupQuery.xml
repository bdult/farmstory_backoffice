<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="itemGroupQuery">

	<select id="list" resultType="hashmap">
		SELECT * FROM BG_ITEM_GROUP
	</select>
	
	<select id="listByBrandId" resultType="hashmap">
		SELECT BIG.* FROM BG_BRAND BB, BG_ITEM_GROUP BIG
		WHERE 1=1
		AND BB.BRAND_ID=BIG.BRAND_ID
		AND BB.BRAND_ID = #{brand_id}
	</select>
	
	<select id="detail" resultType="hashmap">
		SELECT * FROM BG_ITEM_GROUP
		WHERE 1=1
		AND GROUP_ID = #{group_id}
	</select>
	
	<insert id="create" parameterType="map">
		INSERT BG_ITEM_GROUP(
			GROUP_NM,
			BRAND_ID
   		)
   		VALUES(
   			#{group_nm},
   			#{brand_id}
   		)
	</insert>
	
	<update id="modify">
		UPDATE BG_ITEM_GROUP SET
			<if test="brand_id !=null">
			BRAND_ID = #{brand_id},
			</if>
			GROUP_NM = #{group_nm}
		WHERE 1=1
		AND GROUP_ID = #{group_id}
	</update>
	
	<delete id="delete">
		DELETE FROM BG_ITEM_GROUP
		WHERE 1=1
		AND GROUP_ID = #{groupId}
	</delete>
	
</mapper>
