<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="seriseQuery">

	<select id="list" resultType="hashmap">
		SELECT * FROM BG_SERISE
	</select>
	
	<select id="listByBrandId" resultType="hashmap">
		SELECT BS.* FROM BG_BRAND BB, BG_SERISE BS
		WHERE 1=1
		AND BB.BRAND_ID=BS.BRAND_ID
		AND BB.BRAND_ID = #{brand_id}
	</select>
	
	
	
	<insert id="create" parameterType="map">
		INSERT BG_SERISE(
			BRAND_ID,
			CATE_ID,
			SERISE_NM
   		)
   		VALUES(
   			#{brand_id},
   			#{cate_id},
   			#{serise_nm}
   		)
	</insert>
	
</mapper>
