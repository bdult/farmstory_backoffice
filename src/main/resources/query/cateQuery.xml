<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cateQuery">

	<select id="connCheck" resultType="int">
		SELECT 1 FROM DUAL
	</select>
	
	<select id="listAll" resultType="hashmap">
		SELECT * FROM BG_CATE
	</select>
	
	<select id="listByLevel" resultType="hashmap">
		SELECT * FROM BG_CATE
		 WHERE CATE_LEVEL = #{level}
	</select>
	
	<select id="detail" resultType="hashmap">
		SELECT * FROM BG_CATE
		 WHERE CATE_ID = #{cate_id}
	</select>
	
	<insert id="create" parameterType="map">
		
		INSERT /* cateQuery.create 2013.08.07 최지순*/  INTO BG_CATE 
			(CATE_ID,
			CATE_LEVEL,
			CATE_NM,
			MOD_DT)
			VALUES
			(
			#{cate_id},
			#{cate_level},
			#{cate_nm},
			now()
			)
	</insert>
	
	<update id="modyfy">
		UPDATE BG_CATE SET
			CATE_LEVEL= #{cate_level},
			CATE_NM = #{cate_nm},
			PARENT_CATE_ID = #{parent_cate_id},
			MOD_DT = now()
		WHERE 1=1
		AND CATE_ID = #{cate_id}
	</update>
	
	
</mapper>
